/* Обнуляем отступы и задаём шрифты */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    background-color: #F8FAFC; /* общий фон страницы */
    color: #1E293B;
}

.container {
    display: flex;
    min-height: 100vh;
}

/* ---- Левая боковая панель (меню) ---- */
.sidebar {
    width: 280px;
    background-color: #F1F4F9;
    padding: 100px 0 0 30px;
    position: sticky;
    top: 0;
    height: 100vh;
}

.menu {
    display: flex;
    flex-direction: column;
    gap: 24px;
}

.menu-item {
    font-size: 18px;
    font-weight: 500;
    color: #334155;
    text-decoration: none;
    transition: color 0.2s;
}

.menu-item:hover {
    color: #0F172A;
}

.menu-item.active {
    color: #0F172A;
    font-weight: 600;
    position: relative;
}

.menu-item.active::before {
    content: "—";
    position: absolute;
    left: -20px;
    color: #0F172A;
}

/* ---- Правая контентная область ---- */
.content {
    flex: 1;
    max-width: 800px;
    margin: 0 auto;
    padding: 80px 40px 100px 40px;
    background-color: #FFFFFF;
    box-shadow: 0 0 0 1px rgba(0,0,0,0.02), 0 2px 8px rgba(0,0,0,0.02);
}

/* Стили для каждого произведения */
.post {
    margin-bottom: 48px;
}

.post h2 {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    font-weight: 500;
    color: #1E293B;
    margin-bottom: 20px;
    letter-spacing: -0.3px;
}

.post p {
    font-size: 17px;
    line-height: 1.65;
    color: #475569;
    margin-bottom: 20px;
}

/* Разделитель между произведениями */
.divider {
    border: none;
    border-top: 1px solid #E2E8F0;
    width: 80px;
    margin: 0 0 48px 0;
}

/* Адаптивность для маленьких экранов */
max-width: 768px {
    .sidebar {
        width: 200px;
        padding: 60px 0 0 20px;
    }
    .content {
        padding: 40px 24px;
    }
    .post h2 {
        font-size: 28px;
    }
}

max-width: 600px {
    .container {
        flex-direction: column;
    }
    .sidebar {
        width: 100%;
        height: auto;
        position: static;
        padding: 30px 20px;
    }
    .menu {
        flex-direction: row;
        justify-content: center;
        gap: 30px;
    }
    .menu-item.active::before {
        display: none;
    }
    .content {
        padding: 40px 20px;
    }
}